apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: app-ingress
spec:
  rules:
    - http:
        paths:
          - path: /elasticsearch
            pathType: Prefix
            backend:
              service:
                name: elastic-repo-server
                port:
                  number: 80